// import { fromJS } from 'immutable';
// import { makeSelectFormDomain } from '../selectors';

// const selector = makeSelectFormDomain();

describe('makeSelectFormDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false)
  })
})
